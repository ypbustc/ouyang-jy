name: update migu
on: 
  schedule:
    - cron: 30 1 * * *
  workflow_dispatch:
  repository_dispatch:
    types: [mgcron]

jobs:
  main:
    name: update migu
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps: 
    - name: Checkout code
      uses: actions/checkout@v5
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip

        pip install requests
    
    - name: Run migu proxy script
      run: |
        python migu/migu_proxy.py || echo "Script execution completed"
    
    - name: Commit and Push changes
      run: |
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git config --global user.name "github-actions[bot]"
        git add .
        if git diff --cached --quiet; then
          echo "No changes to commit."
        else
          git commit -m "Update migu data: $(date -u +'%Y-%m-%d %H:%M:%S') UTC"
          git push
        fi
